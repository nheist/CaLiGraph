# TODO: For origin, link not only to page but also to section (if available)
# TODO: For types, make sure that they are not transitive
# TODO: Get rid of resources that are Lists, Files, Redirects, ..
# TODO: Create exception for entities in list pages -> directly assign them to the LP-types and add LP as origin

def _extract_page_entities(graph) -> dict:



    return {}
#        'ENTITY_URI1': {'origins': '', 'types': set(), 'in': set(), 'out': set(), 'labels': set()},
#        'ENTITY_URI2': {...}
#    }
